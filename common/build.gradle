architectury {
    common(rootProject.enabled_platforms.split(","))
}

dependencies {
    // Fabric Loader only for the @Environment annotations. Do NOT use other classes.
    implementation "net.fabricmc:fabric-loader:${rootProject.fabric_loader_version}"
}
